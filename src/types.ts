export type Projection = 'iso' | 'persp';
export type RenderMode = 'head' | 'body';
export type Animation = 'stand' | 'idle' | 'walking' | 'running' | 'waving';

export const ANIMATIONS: Animation[] = ['stand', 'idle', 'walking', 'running', 'waving'];
